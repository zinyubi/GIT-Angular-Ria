/**
 * An enumeration describing Path label positions
 */
export declare enum PathLabelPosition {
  /**
   * Vertically align the position of the label such that it is always rendered above the path.
   */
  ABOVE,
  /**
   * Vertically align the position of the label such that it is always rendered below the path.
   */
  BELOW,
  /**
   * Vertically align the position of the label such that it is always rendered below the path.
   */
  CENTER,
}